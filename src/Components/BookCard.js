import React from 'react'

const BookCard = ({book}) => {
    const {id, title, author, image, description} = book; 

  return (
 
        <div>
            
        </div>
    
  );
}

export default BookCard